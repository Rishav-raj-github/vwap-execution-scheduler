# VWAP Execution Scheduler

Executes transaction blocks according to a historical volume curve (U-shaped) to minimize price slippage.

## Setup
Run the scheduler script:
```bash
npm install
npm run dev
```
